services:
  - type: web
    name: emergency-supply-chain-realtime
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    disk:
      name: emergency-supply-chain-data
      mountPath: /opt/render/project/src/.persist
      sizeGB: 1
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: CORS_ORIGIN
        value: "*"
      - key: REDIS_URL
        sync: false
